# .env example for backend
# Database URL (SQLite for dev, PostgreSQL for prod)
DATABASE_URL="file:./dev.db"
# JWT keys (use real RSA keys in production)
JWT_PRIVATE_KEY="supersecret"
JWT_PUBLIC_KEY="supersecret"
# Server port
PORT=3000
