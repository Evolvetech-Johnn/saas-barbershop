# URL pública do backend, incluindo o prefixo /api.
# Local: deixe vazio (usa http://localhost:3001/api por padrão).
# Produção (Vercel): a URL onde o backend (server/) está rodando, ex:
# https://barbearia-saas-backend.up.railway.app/api
VITE_API_URL=""
